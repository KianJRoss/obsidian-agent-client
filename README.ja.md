# Agent Client Plugin for Obsidian (Japanese README Pending Update)

This fork now supports **custom ACP agents only**.

## Quick notes

- Configure agents in **Settings -> Agent Client -> Custom agents**
- Set required env vars in each custom agent config (for example `GEMINI_API_KEY`, `OPENROUTER_API_KEY`)
- CLI-specific built-in integrations are removed

For the latest setup instructions, see [README.md](./README.md).
